[project]
name = "longnav"
version = "0.1"
authors = [
  { name="Avery Xi", email="axi@umich.edu" },
  { name="Qixin Xiao", email="qxiaocs@umich.edu" },

]
description = ""
readme = "README.md"
requires-python = ">=3.10"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
license = "MIT"
license-files = ["LICEN[CS]E*"]
dependencies = [
    "datasets",
    "torch==2.8.0",
    "transformers<5.0",
    "ray[default]==2.53.0",
    "hydra-core",
    "fastapi[standard]",
    "pillow",
    "opencv-python",
    "torchvision",
    "torch",
    "peft",
    "wandb",
    "tensordict",
    "huggingface-hub",
    "codetiming"
]

[project.optional-dependencies]
dev = ["pytest"]

[project.urls]
#Homepage = ""
#Issues = ""

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
markers = [
    "gpu: requires a GPU and the real checkpoint",
    "slow: forward-pass/checkpoint tier",
]