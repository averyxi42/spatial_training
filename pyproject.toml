[project]
name = "longnav"
version = "0.1"
authors = [
  { name="Avery Xi", email="axi@umich.edu" },
  { name="Qixin Xiao", email="qxiaocs@umich.edu" },

]
description = ""
readme = "README.md"
requires-python = ">=3.10"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
license = "MIT"
license-files = ["LICEN[CS]E*"]
dependencies = [
    "ray[default]==2.53.0",
    "hydra-core",
    "regex"
]
[project.optional-dependencies]
vlm = [
    "datasets",
    "torch==2.8.0",
    "transformers<5.0",
    "fastapi[standard]",
    "pillow",
    "opencv-python",
    "torchvision",
    "torch",
    "peft",
    "wandb",
    "tensordict",
    "huggingface-hub",
    "codetiming"
]
# From continuous_action: the RL test tier only needs pytest, kept as its own extra so
# `pip install -e ".[dev]"` does not drag in the whole vlm stack.
dev = ["pytest"]


[project.urls]
#Homepage = ""
#Issues = ""

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
markers = [
    "gpu: requires a GPU and the real checkpoint",
    "slow: forward-pass/checkpoint tier",
]
